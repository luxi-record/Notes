### 初级前端知识点学习记录
1. **in 和 hasOwnProperty的区别**
   
   in既可以访问自身属性也可以实例原型上的属性，而hasOwnProperty只能访问自身的属性
   ```javascript
   Object.prototype.name = 'name';
   const obj = { age: 22 };
   console.log('name' in obj); // true
   console.log('age' in obj); // true
   console.log(obj.hasOwnProperty('name')); // false
   console.log(obj.hasOwnProperty('age')); // true
   ```
2. **instanceof的使用**
   
   instanceof用于判断构造函数的原型是否存在于实例的原型链上，它会一直找到Object.prototype的__proto__，如果在此之前找到了就返回true，如果找到这都还没有就返回false
   ```javascript
   function Father(name) {
     this.name = name;
   };
   function Son(age) {
     this.age = age;
   };
   Son.prototype = new Father();
   Son.prototype.constructor = Son;
   const son = new Son('son', 18);
   const father = new Father('father');
   console.log(son instanceof Son); // true
   console.log(son instanceof Father); // true,
   /*为啥这个判断也是true
   * 第一次查找 Father.prototype === son.__proto__; 不相等，继续查找son.__proto__.__proto__，也就是Son.prototype.__proto__
   * Son.prototype指向Father的一个实例， 所以相当于是 const father = new Father(); Son.prototype = father
   * 第二次查找 Father.prototype === son.__proto__.__proto__ === Son.prototype.__proto__ === father.__proto__
   */
   console.log(father instanceof Son); // false
   console.log(father instanceof Father); //true
   ```
3. **什么是原型链**
   
   每一个对象实例都有一个__proto__原型链属性，这个属性指向其构造函数的原型Constructor.prototype，当访问一个实例的属性时候如果其本身不存在该属性，就会沿着原型链（一直访问__proto__）一直向上查找如果找到了就返回具体的值，如果直到Object.prototype.__proto__都没有找到就会返回undefined。
   ```javascript
   Object.prototype.name = 'name';
   function Father(age) {
     this.age = age;
   };
   Father.prototype.sex = '男';
   const obj = new Father(22);
   console.log(obj.age); // 22 访问自身属性obj.age
   console.log(obj.sex); // 男
   /*obj上无sex属性，无往上找原型链上是否存在，也就是obj.__proto__上是否存在，而obj.__proto__ = Father.prototype
   * Father.prototype.sex = '男'   找到了‘男’返回
   */
   console.log(obj.name); // name
   /*obj上无name属性，无往上找原型链上是否存在，也就是obj.__proto__上是否存在，而obj.__proto__ = Father.prototype
   * Father.prototype.name 无name属性继续往上找，也就是Father.prototype.__proto__，而Father.prototype.__proto__ = Object.prototype
   * Object.prototype.name = 'name';  找到了‘name’返回
   */
   ```
4. **继承的方式**
   
   在js中常见的继承方式有几种，每一种都有自己的优缺点。
   
   ***其实在js的继承中，主要分为三种情况，一种只继承父类的实例属性，一种只继承父类的原型属性，一种是既继承父类的实例属性也要继承父类原型属性。***
   
   ***继承父类实例属性就是类似构造函数继承，继承父类的原型属性类似原型链条继承，两种都继承的话就是类似组合继承***
   
   ***尽量别继承父类原型上的属性（除方法），因为所有子类实例都是共享一份父类原型，所以一旦有修改，所有实例去访问的时候都会被修改***
   ```javascript
   // 原型链继承
   function Father(age) {
     this.age = age;
   };
   function Son(name) {
     this.name = name;
   };
   Son.prototype = new Father('default'); // 继承
   Son.prototype.constructor = Son; // 纠正构造函数指向
   Son.prototype.fn = fn; // 重新方法
   const son = new Son('son');
   const son2 = new Son('son2');
   /*缺点：
   * 1.在创建实例时候不能给父类传递参数
   * 2.所有实例都公用同一个父类的实例属性，导致son修改了name后son2去访问会得到修改后的值
   */

   // 构造函数继承
   function Father(age) {
     this.age = age;
   };
   function Son(name, age) {
     Father.call(this, age) // 继承父类除原型的属性
     this.name = name;
   };
   /*缺点：
   * 解决了无法向父类传参的问题，但是子类实例无法访问父类的原型上的属性，也是就Father.prototype
   */

   // 组合继承
   function Father(age) {
     this.age = age;
   };
   function Son(name, age) {
     Father.call(this, age) // 继承父类除原型的属性
     this.name = name;
   };
   Son.prototype = new Father() // 继承父类原型属性
   Son.prototype.constructor = Son; // 纠正构造函数指向
   /*缺点：
   * 解决了无法向父类传参的问题以及无法继承父类原型属性的问题，但是调用了两次父类构造函数，效率低
   */

   // 原型式继承
   function Father(age) {
     this.age = age;
   };
   const fatherPrototype = { } //定义父类的原型
   function Son(name, age) {
     Father.prototype = fatherPrototype
     return new Father();
   };
   /*缺点：
   * 这种相当于显示的定义了父类的原型，存在和原型链继承类似的问题
   */

   // 寄生继承
   function Father(age) {
     this.age = age;
   };
   function Son(name, age) {
     const clone = new Father(age);
     clone.name = name; // 相当于是对父类的增强
     return clone
   };
   /*缺点：
   * 所有实例也是共享一份原型上的数据
   */

   // 寄生组合继承
   function Father(age) {
     this.age = age;
   };
   function Son(name, age) {
     Father.call(this, age);
   };
   Son.prototype = Object.create(Father.prototype)
   Son.prototype.constructor = Son;
   /*
   * 类似组合继承
   */
   ```
5. **什么是尾调用，以及它的优点**
   
   当一个函数执行的最后一步是返回另一个函数的调用结果，而不是在调用函数之后还有其他操作，这就是尾调用，他有以下好处：
   
   内存优化：最大的好处是**节省调用栈的空间**。在支持尾调用优化(TCO)的环境中，尾调用不会创建新的调用帧(stack frame)，而是重用当前函数的调用帧。
   
   防止栈溢出：对于递归函数尤其重要。普通递归可能因为调用层级过深导致栈溢出(stack overflow)错误，而尾递归通过优化可以避免这个问题。
   
   性能提升：由于**不需要保存和恢复额外的调用栈信息，执行速度可能更快。**
   ```javascript
   // 这是尾调用的例子
   function foo() {
     return bar(); // 最后一步是调用 bar 并直接返回其结果
   }
   // 这不是尾调用
   function notTail() {
     const result = bar(); // 调用 bar
     return result; // 返回保存的结果，不是直接返回函数调用
   }
   // 这也不是尾调用
   function notTailEither() {
     return bar() + 1; // 在 bar() 返回后还有 +1 操作
   }
   ```
6. **数据类型&隐式转化**

   在js的运算过程中会发生一些隐式转化，转化主要分为三大类，转数字，转字符串，转布尔值。在了解转化之前我们需要知道js数据类型分为两大类，一是复杂数据类型，一是简单数据类型。
   
   简单数据类型主要有七大类： number，string，boolean，null，undefined, Symbol，BigInt；

   复杂数据类型：Object， Array，Function等等各种构造函数
   
   **各种数据转数字**：
      1. 字符串转数字：如果字符串是一个**纯数字输出这个数字**，如果是**数字加字符**或者**纯字符**转为**NaN**，**空字符串输出0**；
      2. 布尔值转数字：**false 输出 0**， **true 输出 1**；
      3. null和undefined转数字： **null 输出 0**， **undefined 输出 NaN**；
      4. 各种对象转数字：**首先调用对象的valueOf()方法，如果valueOf()输出了原始值（数字，字符串，布尔，null，undefined, Symbol,BigInt），那就把原始值转化为数字，如果valueOf()输出的是对象，那么就会调用对象的toString()方法，然后将toString()返回的值转化为数字**；

   **各种数据转字符串**：
      1. null转字符串： "null"
      2. undefined转化字符串： "undefined"
      3. Symbol转字符串： 'Symbol('str')'
      4. 数字转字符串： "2"
      5. 复杂数据类型：复杂数据类型通常是**调用其toString()方法**，在没有修改其toString方法的前提下：**数组：会把数组的每一项都转化为字符串，然后通过逗号拼接在一起，空数组则是空字符串**，**对象： '[object Object]'**, **函数：如果是自定义的函数则转化为字符串，如果是原生方法则是function() {[native code]}**。

   **各种数据转布尔值**：
      1. null 和 undefined： false；
      2. 字符串转布尔： 空字符串为 false，非空为 true；
      3. 各种对象和数组： true；
      4. 数字转布尔： 0 -0 +0 NaN 都为false，除此之外都为true；

   对于对象我们可以通过定义其toString()或者valueOf()方法自定义转换，也可以通过[Symbol.toPrimitive]属性定于其转为，如果[Symbol.toPrimitive]和 toString() valueOf() 两种同时存在的话会以[Symbol.toPrimitive]为转化规则。
   ```javascript
   // 对象自定义转化 [Symbol.toPrimitive]
   const obj = {
     [Symbol.toPrimitive](hint) {
       // hint 可能是 "number"、"string" 或 "default"
       if (hint === "number") return 42;
       if (hint === "string") return "hello";
       return true; // default
     }
   };
   // 对象自定义转化 toString, valueOf
   const obj = {
     toString() { return 'hello' },
     valueOf() { return 42 }
   };
   // 常见的一些隐式转化
   + data; // 把数据转化为数字
   
   `${data}`; // 转化为字符串
   
   !!data; // 转化为布尔值
   
   {} + 1; // 对象 + 数字 ==> 会对各种对象走一遍对象转化数字的逻辑 valueOf() ? valueOf() : toString();
   
   'str' + 1; // 字符 + 数字 ==> 会把数字转为字符串再拼接；
   
   if(data){}; // 都转化为布尔值；
   
   // -, *, /, % 这四个运算符都会把数据转化为数字；
   
   /* 比较运算符 <, >, <=, >=。
   *  如果两个操作数都是字符串，按字典顺序比较
   *  否则，尝试将操作数转换为数字进行比较
   */

   /* 相等运算符 (==). 相等运算符在比较前会进行类型转换
   *  如果一个操作数是布尔值，将其转换为数字
   *  如果一个操作数是字符串，另一个是数字，将字符串转换为数字
   *  如果一个操作数是对象，另一个不是，调用对象的 ToPrimitive 方法
   *  null 和 undefined 相等比较为 true
   */
   "42" == 42    // true (字符串转为数字)
   true == 1     // true (布尔值转为数字)
   null == undefined // true (特殊规则)
   "0" == false  // true (false转为0，"0"转为0)
   [] == ""      // true ([]转为"")
   [] == 0       // true ([]转为""，""转为0)
   [2] == 2      // true ([2]转为"2"，"2"转为2)
   ```
6. **一系列手写题**
   ```javascript
   // 手写instanceof（主要考察队它的理解，instanceof是判断构造函数的原型是否存在于对象的原型链上）
   const myInstanceof = (left, right) => {
      const prototype = right.prototype
      let proto = Object.getPrototypeOf(left);
      while(proto) {
         if(proto === prototype) return true;
         proto = Object.getPrototypeOf(proto);
      }
      return false;
   }
   // 手写new的实现(new会创建一个新对象，并把构造函数的原型设置到对象的原型链上，特别注意：如果构造函数返回一个对象，那么new就返回这个对象)
   const myNew = (constructor, ...args) => {
      const obj = {};
      Object.setPrototypeOf(obj, constructor.prototype);
      const result = constructor.call(clonePrototy, ...args);
      return (result && typeof result === 'object') ? result : obj;
   }
   // 手写深拷贝
   const deepClone = (obj, map = new WeakMap()) => {
    // 处理基本数据类型和null
    if (obj === null || typeof obj !== 'object') {
        return obj;
    }
    // 处理循环引用
    if (map.has(obj)) {
        return map.get(obj);
    }
    // 处理Date对象
    if (obj instanceof Date) {
        return new Date(obj);
    }
    // 处理RegExp对象
    if (obj instanceof RegExp) {
        return new RegExp(obj);
    }
    // 处理数组和对象
    const cloned = Array.isArray(obj) ? [] : {};
    map.set(obj, cloned); // 先设置映射，防止循环引用
    // 递归拷贝所有属性
    for (let key in obj) {
        if (obj.hasOwnProperty(key)) {
            cloned[key] = deepClone(obj[key], map);
        }
    }
    // 处理Symbol属性
    const symbolKeys = Object.getOwnPropertySymbols(obj);
    for (let symKey of symbolKeys) {
        cloned[symKey] = deepClone(obj[symKey], map);
    }
    return cloned;
   }
   // 防抖
   
   ```
7. **一些偏僻的知识**
   ```javascript
   // 在构造函数中new.target指向构造函数本身
   function Father(arg) {
      // 如果调用Father时候没有通过new Father()的方式，new.target为undefined
      if(!new.target) {
         return new Father(arg);
      }
   }
   const fa = Father();
   const fa2 = new Father();
   
   // isNaN和Number.isNaN的区别
   isNaN('str'); // true, 这是应为它会先把数据转化为number，再判断
   Number.isNaN('str'); // false，他不会转化数据而是直接判断
   
   // undefined和null转化为数字
   // 不论是隐式转化还是显示转化undefined永远为NaN，null永远是0
   // 解释：null代表是有值但是是一个空值，而undefined代表是无值
   + undefined; //输出NaN
   + null; //输出0
   
   // 函数中的length表示参数的长度，箭头函数也有length只是没arguments
   function foo(a, b, c = 1) {}
   foo.length; // 2，不计算默认参数
   
   // 空槽数组
   const arr = [1, , 3];
   arr.length; // 3
   arr.map(x => x * 2); // [2, undefined, 6] (map会跳过空槽)
   [...arr]; // [1, undefined, 3] (扩展运算符将空槽视为undefined)
   
   // set数据结构可以去重NaN
   const set = new Set([NaN, NaN]);
   set.size; // 1 (Set认为NaN等于自身)
   
   // Promise空数组
   Promise.race([]); // 永远处于pending状态
   Promise.all([]); // 立即解析为[]

   // 闭包的优化
   function createLeak() {
     const largeData = new Array(1000000).fill('x');
     return function leaky() {
       // 即使只使用了一个字符，整个largeData仍被引用
       console.log(largeData[0]);
     };
   }
   // 优化版本
   function createOptimized() {
     const largeData = new Array(1000000).fill('x');
     const firstItem = largeData[0]; // 只保留需要的数据
     return function optimized() {
       console.log(firstItem);
     };
   }
   ```
